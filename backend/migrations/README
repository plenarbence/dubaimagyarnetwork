Generic single-database configuration.

új mező, tábla, módosítás	alembic revision --autogenerate -m "msg"
változás alkalmazása		alembic upgrade head
visszagörgetés (rollback)	alembic downgrade -1


megnyitni a database.db-t 	sqlite3 backend/database.db